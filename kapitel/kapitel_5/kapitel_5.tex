\newpage
\section{Durchführung und Evaluation}

\subsection{Durchführung des Experiments}

Die Generierungsabläufe aller Stufen fanden zwischen dem 25. und 27. Juli 2026 statt. Modellkonfiguration und Stand der Demo-Anwendung blieben über diesen Zeitraum unverändert. Verwendet wurden exakt die Parameter aus Kapitel~4.4.

Vorgesehen waren pro Stufe 500 Testdateien, allerdings wurde diese Anzahl nur in den Stufen 1, 2, 4 und 5 erreicht. Stufe~3 umfasst nur 499 Dateien, da im Durchlauf run\_20 der Test zu UC-02 fehlt. Da weder die Testdatei noch der zugehörige Prompt oder die Rohantwort des Modells geschrieben wurde, lässt sich der Fehler auf den Aufruf des Modells eingrenzen, denn erst nach Rückgabe der Antwort werden alle drei Dateien angelegt. Nach einem fehlgeschlagenen Aufruf macht das Skript mit dem nächsten Use Case weiter, statt den gesamten Durchlauf abzubrechen. Die genaue Ursache ist nachträglich nicht mehr bestimmbar, da die Konsolenausgabe nicht gespeichert wurde.

Ausgeführt wurden die Tests mit Playwright~1.60.0 und dem darin enthaltenen Chromium unter Node.js~26.5.0 und pnpm~11.1.2. Alle Tests liefen nacheinander gegen die unter \url{http://localhost:5173/ba-webgis-llm-e2e/} lokal laufende Demo-Anwendung.

Die semantische Bewertung nach Kapitel~4.6.2 wurde zwischen dem 31. Juli und dem 3. August 2026 mit Claude Opus~5 über Claude Code~2.1.220 durchgeführt.

Die Kennzahlen dieses Kapitels wurden skriptgestützt berechnet\footnote{\url{https://github.com/nils-gb156/ba-webgis-llm-e2e/tree/qwen/test-generation/src/app/llm/eval_extract}} und beinhalten die Auswertungen der beiden Evaluationsphasen aus den Ergebnisdateien sowie die Auszählung wiederkehrender Codemuster durch reguläre Ausdrücke über alle generierten Testdateien. Die Skripte wurden ebenfalls mit einem Prompt\footnote{\url{https://github.com/nils-gb156/ba-webgis-llm-e2e/blob/qwen/test-generation/src/app/llm/phase2_judge_evaluation_prompt.md}} in Claude Code direkt im Repository mit Opus~5 erstellt. In dieser Arbeit werden nur die wichtigsten Befunde dargestellt. Die vollständigen Auswertungsberichte\footnote{\url{https://github.com/nils-gb156/ba-webgis-llm-e2e/tree/qwen/test-generation/docs/eval}} liegen im Repository.

Zusätzlich wurde der komplette Generierungsablauf mit GPT-5.4 statt Qwen3.6-35B-A3B durchgeführt (Ergebnisse siehe Anhang~\ref{app:results-gpt}).

Die vier Stufen unterscheiden sich, wie in Kapitel~4.3 beschrieben, nicht nur im Informationsgehalt, sondern auch im Aufwand ihrer Vorbereitung. Stufe~1 erfordert keine Vorbereitung. Der Accessibility-Snapshot in Stufe~2 und die generierte \ac{UI}-Map in Stufe~3 werden vor dem Generierungslauf skriptgestützt erzeugt und laufen innerhalb weniger Sekunden durch. Die manuelle \ac{UI}-Map für Stufe~4 wurde in ca. 2 h 15 min erstellt. Dies umfasst das Sichten der Komponenten, das Erfassen der Selektoren sowie die Erstellung der Hierarchie und der bedingten Sichtbarkeiten. Hinzu kommt, dass sie bei jeder Änderung an Selektoren, Komponentenstruktur oder Sichtbarkeitsbedingungen händisch aktualisiert werden muss.

\subsection{Ergebnisse der Ausführung (Phase 1)}

Grundlage der Auswertung sind 1.999 Testdateien der Stufen 1 bis 4. Die Ergebnisse der Bonus-Stufe~5 werden wegen des abweichenden Ausführungsmodells getrennt in Kapitel~6.4 dargestellt. Jede Datei wurde einer der sechs Kategorien aus Kapitel~4.6.1 zugeordnet. Zehn Dateien entsprachen keinem Muster der regelbasierten Zuordnung, weil sie wegen ihrer Fehler nicht richtig ausgeführt werden konnten, und wurden daher vorläufig als \texttt{TIMEOUT} klassifiziert. Die manuelle Prüfung ergab in neun Fällen einen Syntaxfehler (\texttt{COMPILE\_ERROR}) und in einem Fall eine Wiederholung ganzer Codeblöcke (\texttt{GENERATION\_ERROR}).

Tabelle~\ref{tab:phase1_kategorien} zeigt die Ausführungsergebnisse. Die PASS-Rate steigt monoton von 20,0\,\% in Stufe~1 auf 38,6\,\% in Stufe~4. Der größte Einzelschritt liegt mit 13,7 Prozentpunkten zwischen Stufe~2 und Stufe~3, also bei der Einführung der \ac{UI}-Map mit Informationen über die gesamte Web-Anwendung und der Map-Model-Helfer. Die Übergänge von Stufe~1 zu 2 (+2,0 Prozentpunkte) und von Stufe~3 zu 4 (+2,9 Prozentpunkte) verändern die PASS-Rate dagegen kaum.

\begin{table}[H]
    \centering
    \caption[Ausführungsergebnisse je Stufe]{Ausführungsergebnisse der Stufen 1 bis 4 nach Kategorien (Phase 1), absolut und in Prozent der Stufengrundmenge}
    \label{tab:phase1_kategorien}
    \begin{tabular}{lrrrr}
        \toprule
        Kategorie & Stufe 1 & Stufe 2 & Stufe 3 & Stufe 4 \\
        \midrule
        \textsc{pass}             & 100 (20{,}0\,\%) & 110 (22{,}0\,\%) & 178 (35{,}7\,\%) & 193 (38{,}6\,\%) \\
        \textsc{infra\_fail}     & 345 (69{,}0\,\%) & 211 (42{,}2\,\%) & 133 (26{,}7\,\%) & 145 (29{,}0\,\%) \\
        \textsc{assertion\_fail}  & 43 (8{,}6\,\%)   & 166 (33{,}2\,\%) & 184 (36{,}9\,\%) & 154 (30{,}8\,\%) \\
        \textsc{compile\_error}  & 3 (0{,}6\,\%)    & 1 (0{,}2\,\%)    & 3 (0{,}6\,\%)    & 4 (0{,}8\,\%)    \\
        \textsc{generation\_error} & 9 (1{,}8\,\%)  & 12 (2{,}4\,\%)   & 1 (0{,}2\,\%)    & 4 (0{,}8\,\%)    \\
        \textsc{timeout}         & 0                & 0                & 0                & 0                \\
        \midrule
        $n$                      & 500              & 500              & 499              & 500              \\
        \bottomrule
    \end{tabular}
\end{table}

Zwischen Stufe~1 und 2 sinkt der \texttt{INFRA\_FAIL}-Anteil von 69,0\,\% auf 42,2\,\%, während der \texttt{ASSERTION\_FAIL}-Anteil im selben Schritt von 8,6\,\% auf 33,2\,\% steigt. Der Accessibility-Snapshot verbessert demnach das Auffinden der Elemente, sodass die Tests nicht mehr am Selektor scheitern, sondern erst an der inhaltlichen Prüfung. Nicht bei allen Use Cases tritt diese Verschiebung gleich stark auf (Abbildung~\ref{fig:exec_by_uc}). Bei UC-06, UC-07 und UC-08 wandelt sich der in Stufe~1 fast vollständige \texttt{INFRA\_FAIL}-Anteil bis Stufe~2 überwiegend in \texttt{ASSERTION\_FAIL} um, bei UC-09 und UC-10 fällt diese Verschiebung deutlich geringer aus.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{exec_category_by_uc_grid}
    \caption[Ausführungskategorien je Use Case und Stufe]{Ausführungskategorien je Use Case, Stufen 1 bis 4 (Phase 1)}
    \label{fig:exec_by_uc}
\end{figure}

Innerhalb der Stufe~4 reicht die PASS-Rate von 2\,\% (UC-07) bis 98\,\% (UC-01). Use Cases mit einfachen Sichtbarkeitsprüfungen wie UC-01 und UC-05 erreichen bereits in den unteren Stufen hohe Werte, während die karteninteraktiven UC-06, UC-07 und UC-08 in allen vier Stufen unter 25\,\% bleiben.

Über die Stufen hinweg steigt die PASS-Rate bei den meisten Use Cases an. UC-03 ist der einzige Use Case, dessen PASS-Rate dabei deutlich schlechter wird: 62\,\% in Stufe~1, 32\,\% in Stufe~2, 4\,\% in Stufe~3 und 14\,\% in Stufe~4. UC-04 bricht von Stufe~3 auf Stufe~4 von 66\,\% auf 12\,\% ein, obwohl die Gesamtrate in diesem Schritt steigt. UC-02 erreicht in Stufe~3 keinen einzigen PASS.

Der Rückgang bei UC-03 lässt sich auf einen Fehler bei der Prüfung des Zoomverhaltens zurückführen. Ab Stufe~3 fragen die Tests den numerischen Zoomwert über \code|getMapZoomLevel| ab. In Stufe~3 entfallen 58,3\,\% der Fehlschläge dieses Use Cases auf denselben Match-Fehler (\enquote{expected value must be a number or bigint}), in Stufe~4 sind es 53,5\,\%. Die Ursache zeigt der generierte Code in Listing~\ref{lst:uc03_generated}: Die Tests weisen den Rückgabewert von \code|expect.poll(...)| einer Variablen zu, um ihn im nächsten Schritt als Vergleichswert zu verwenden. Da \code|expect.poll| als Assertion aber keinen Wert zurückgibt, erhält die Variable \code|undefined|, und der Vergleich scheitert. Der manuelle Referenztest in Listing~\ref{lst:uc03_reference} fragt den Zoomwert vor der Interaktion direkt über \code|getMapZoomLevel(page)| ab und setzt \code|expect.poll| nur zum Warten auf die Zustandsänderung ein, ohne dessen Rückgabewert weiterzuverwenden.

\begin{listing}[H]
\caption{Fehlerhaftes Muster in den generierten Tests (gekürzt, Stufe 4, run\_01)}
\label{lst:uc03_generated}
\begin{minted}{typescript}
const initialZoom = await expect.poll(() => getMapZoomLevel(page)).toBeTruthy();
await page.getByTestId('zoom-in-button').click();
const zoomedInLevel = await expect.poll(() => getMapZoomLevel(page)).toBeGreaterThan(initialZoom);
\end{minted}
\end{listing}

\begin{listing}[H]
\caption{Korrektes Muster im manuellen Referenztest (gekürzt)}
\label{lst:uc03_reference}
\begin{minted}{typescript}
const initialZoom = await getMapZoomLevel(page);
await page.getByTestId("zoom-in-button").click();
await expect.poll(() => getMapZoomLevel(page)).toBeGreaterThan(initialZoom as number);
\end{minted}
\end{listing}

In den Stufen~1 und 2, in denen die Map-Model-Helfer nicht im Kontext stehen, prüfen die generierten Tests für UC-03 stattdessen nur Sichtbarkeiten im \ac{DOM}. Die Prüfung des Zoomlevels wird nur als Kommentar aufgeführt, und dadurch laufen die Tests häufiger durch.

Die übrigen Kategorien machen nur einen geringen Anteil aus. \texttt{COMPILE\_ERROR} und \texttt{GENERATION\_ERROR} machen zusammen je Stufe zwischen 0,8\,\% und 2,6\,\% aus. Abgeschnittene oder degenerierte Modellausgaben (\texttt{GENERATION\_ERROR}) treten fast nur in den Stufen 1 und 2 auf (9 und 12 Fälle) und gehen danach auf 1 (Stufe~3) und 4 (Stufe~4) zurück. Die vier Kompilierfehler in Stufe~4 gehen alle auf einen falschen relativen Importpfad der Helferdatei in UC-06 zurück.

\subsection{Ergebnisse der Qualitätsbewertung (Phase 2)}

Grundlage sind je Stufe die 488 bis 498 Dateien, die nach Abzug der \texttt{GENERATION\_ERROR}-Fälle semantisch bewertet wurden. Die Dimension \emph{map\_interaction} ist nur für die kartenrelevanten Use Cases UC-04, UC-06, UC-07, UC-08 und UC-10 anwendbar. Tabelle~\ref{tab:phase2_dimensionen} zeigt Median und Mittelwert je Dimension und Stufe.

\begin{table}[H]
    \centering
    \caption[Bewertungsdimensionen je Stufe]{Median und Mittelwert der vier Bewertungsdimensionen je Stufe (Phase 2, Skala 1--4)}
    \label{tab:phase2_dimensionen}
    \begin{tabular}{lrrrrrrrr}
        \toprule
        & \multicolumn{2}{c}{coverage} & \multicolumn{2}{c}{selector} & \multicolumn{2}{c}{map\_interaction} & \multicolumn{2}{c}{assertion} \\
        Stufe & Md & Ø & Md & Ø & Md & Ø & Md & Ø \\
        \midrule
        1 & 4 & 3{,}68 & 2 & 2{,}48 & 1 & 1{,}40 & 4 & 3{,}29 \\
        2 & 4 & 3{,}73 & 3 & 3{,}00 & 2 & 1{,}76 & 4 & 3{,}26 \\
        3 & 4 & 3{,}72 & 4 & 3{,}13 & 3 & 2{,}85 & 4 & 3{,}56 \\
        4 & 4 & 3{,}91 & 4 & 3{,}41 & 3 & 3{,}01 & 4 & 3{,}59 \\
        \bottomrule
    \end{tabular}
\end{table}

Die Dimensionen \emph{coverage} und \emph{assertion} haben über alle vier Stufen den Median 4. Der Mittelwert steigt bei \emph{coverage} nur geringfügig von 3,68 auf 3,91 und bei \emph{assertion} von 3,29 auf 3,59. Ob ein Test die geforderten Schritte abdeckt und inhaltlich sinnvoll prüft, hängt demnach stärker vom Verständnis des Use Cases ab als vom bereitgestellten \ac{UI}-Kontext.

Ein deutlicher Stufenunterschied zeigt sich dagegen in \emph{selector} und \emph{map\_interaction}. Der Median von \emph{selector} steigt bereits zwischen Stufe~1 und 2 von 2 auf 3 und erreicht ab Stufe~3 den Wert 4. Der größte Sprung liegt hier beim Übergang zum Accessibility-Snapshot in Stufe~2 (Ø +0,52). Der Median von \emph{map\_interaction} bleibt in Stufe~2 noch bei 2 und steigt erst ab Stufe~3 mit der Einführung der Map-Model-Helfer auf 3 (Ø +1,09). Die Nutzung einer Helferfunktion springt im selben Schritt von keiner auf 416 Dateien. Beide Dimensionen reagieren also auf unterschiedliche Bestandteile des Kontexts: \emph{selector} auf den erstmals bereitgestellten \ac{UI}-Kontext durch den Accessibility-Snapshot und \emph{map\_interaction} auf die Map-Model-Helfer.

In Stufe~1 sind 33 der 100 PASS-Fälle (33\,\%) als \texttt{vacuous\_pass} markiert, davon 31 allein bei UC-03. In den übrigen Stufen liegt der Anteil nur noch zwischen 2,7 und 7,8\,\% und betrifft vor allem UC-08. Der hohe Wert bei UC-03 in Stufe~1 bestätigt den beschriebenen Befund aus Abschnitt~6.2. Ohne Kenntnis der Map-Model-Helfer prüfen die generierten Tests dort nur Sichtbarkeiten im \ac{DOM} und haben dadurch eine höhere PASS-Rate, ohne das im Use Case geforderte Zoomverhalten tatsächlich zu verifizieren. Die im Footer angezeigte Maßstabsleiste bietet eine alternative Prüfmöglichkeit, die in dieser Stufe jedoch kein einziger Test nutzt.

Auch innerhalb einer Stufe streuen die Dimensionswerte über die Use Cases (Abbildung~\ref{fig:score_heatmap}). Der Widerspruch aus hohem \emph{selector}- und niedrigem \emph{assertion}-Wert, der bei UC-03 in Stufe~1 auftritt, findet sich in gleicher Form auch in Stufe~4 wieder: \emph{coverage} und \emph{selector} erreichen dort 4,0, \emph{assertion} bleibt bei 2,3. Bei UC-07 schwankt \emph{map\_interaction} über alle Stufen zwischen 1,4 und 2,3. Ein Anstieg durch die Map-Model-Helfer ist im Vergleich zu den anderen Use Cases nicht zu erkennen. Das liegt daran, dass die Helfer für diesen Fall keine Funktion bereitstellen, da es um einen gezielten Klick auf eine Koordinate in der Karte geht.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{score_heatmap_grid}
    \caption[Mittlerer Judge-Score je Use Case und Dimension]{Mittlerer Judge-Score je Use Case und Dimension, Stufen 1 bis 4 (Phase 2)}
    \label{fig:score_heatmap}
\end{figure}

\subsubsection{Kontrolle der maschinellen Bewertung}

Die Kontrolle der 50 Dateien der Stichprobe mit 175 bewertbaren Einzelurteilen ergab, dass 149 (85,1\,\%) als bestätigt, 18 als grenzwertig, aber vertretbar, und 8 als abweichend eingestuft wurden. Am niedrigsten liegt die Bestätigungsquote in der Dimension \emph{assertion} (38 von 50), am höchsten in \emph{coverage} (44 von 50) und \emph{selector} (45 von 50). Tabelle~\ref{tab:review_by_stage} fasst das Ergebnis je Stufe zusammen, und die vollständige Aufstellung der Einzelurteile enthält Anhang~\ref{app:review}.

\begin{table}[H]
    \centering
    \caption[Ergebnis der Kontrolle je Stufe]{Ergebnis der Kontrolle je Stufe (Dimensionen zusammengefasst)}
    \label{tab:review_by_stage}
    \begin{tabular}{lrrrr}
        \toprule
        Stufe & bestätigt & grenzwertig & abweichend & $n$ \\
        \midrule
        1 & 28 & 6 & 1 & 35 \\
        2 & 28 & 5 & 2 & 35 \\
        3 & 29 & 4 & 2 & 35 \\
        4 & 30 & 3 & 2 & 35 \\
        5 & 34 & 0 & 1 & 35 \\
        \midrule
        gesamt & 149 & 18 & 8 & 175 \\
        \bottomrule
    \end{tabular}
\end{table}

Auffällig ist, dass die Begründungen der Scores auf einen nummerierten Regelkatalog verweisen, der im Prompt nicht vorgegeben wurde. In der Stichprobe betrifft das 18 der 50 Dateien, verteilt auf alle Stufen außer Stufe~3. Bemerkenswert ist dabei, dass dieselbe Nummer stufenübergreifend für den gleichen Sachverhalt steht. So bezeichnet Regel~16 in den Stufen 1, 2, 4 und 5 jeweils eine falsche Koordinatenlogik und taucht nur bei UC-07 auf. Das Modell muss diese Regeln während der Bewertung selbst gebildet und über die einzelnen Läufe hinweg angewendet haben, obwohl die Bewertung jeder Stufe in einem eigenen Kontextfenster stattfand.

Zudem werden gleiche Befunde nicht durchgängig gleich bewertet. Zwei Dateien der Stufe~2 prüfen die im Use Case geforderten 24 Forecast-Einträge über einen Zähler: einmal über beliebige \code|div|-Elemente, einmal über eine Rolle, die die Einträge nicht enthält. Keine der beiden Zählungen erfasst damit die im Use Case geforderte Bedingung. In \emph{assertion} erhält die erste Datei dennoch den Wert 1 und die zweite den Wert 4, mit der Begründung, dass eine Zählprüfung auch mit erfundenem Lokator anzuerkennen sei, während dieselbe Zählprüfung im ersten Fall als Argument für den niedrigsten Wert dient. Im Prompt steht jedoch keine Vorgabe, die erfundene Lokatoren zulässt.

\subsection{Ergebnisse der Bonus-Stufe 5}

Von den 500 Läufen erreichen 366 (73,2\,\%) ein bestandenes Ausführungsergebnis und damit mit Abstand den höchsten Wert aller Stufen (+34,6 Prozentpunkte zu Stufe~4). Die Kategorie \texttt{INFRA\_FAIL} fällt auf 15 Fälle, \texttt{COMPILE\_ERROR} auf null, \texttt{GENERATION\_ERROR} auf 1, während \texttt{ASSERTION\_FAIL} mit 118 die restlichen Fehlschläge nahezu vollständig ausmacht. Die Use Cases UC-01, UC-02, UC-04 und UC-05 bestehen sogar in allen 50 Läufen, kein Use Case bleibt bei null. Am unteren Ende liegen UC-03 mit 18\,\% und UC-08 mit 28\,\%. Abbildung~\ref{fig:exec_category_by_uc_stage5} zeigt die Verteilungen der Ausführungskategorien.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{exec_category_by_uc_stage5}
    \caption[Ausführungskategorien je Use Case der Bonus-Stufe 5]{Ausführungskategorien je Use Case der Bonus-Stufe 5 (Phase 1)}
    \label{fig:exec_category_by_uc_stage5}
\end{figure}

Den größten Teil dieses Zugewinns liefern die ersten beiden Iterationen (Abbildung~\ref{fig:stufe5_loop_ergebnis}). Bereits in Iteration~0 ohne Korrektur bestehen 146 Läufe (29,2\,\%), nach dem ersten Durchlauf (Iteration~1) sind es zusammen 274 (54,8\,\%). Ab rund 70\,\% (Iteration~5) verläuft die Kurve nahezu flach und endet bei 73,2\,\%. 136 Läufe erreichen die letzte Iteration, von denen nur zwei bestehen. Bestandene Läufe benötigen im Mittel 2,22 Iterationen.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{stufe5_loop_grid}
    \caption[Konvergenz des Self-Improvement-Loops]{Kumulierte PASS-Rate nach Iteration (links) und Iteration des Bestehens je Use Case (rechts), Bonus-Stufe~5}
    \label{fig:stufe5_loop_ergebnis}
\end{figure}

Zwischen den Iterationen bleibt die Fehlerklasse überwiegend erhalten. Von den 1.654 Korrekturaufrufen, die aus einem vorherigen Fehlschlag entstanden, führen 1.197 (72,4\,\%) in dieselbe Klasse zurück. Aus \texttt{ASSERTION\_FAIL} führen 83,3\,\% der Übergänge wieder zu \texttt{ASSERTION\_FAIL}, nur 9,2\,\% zu \texttt{PASS} und 7,4\,\% zu \texttt{INFRA\_FAIL} oder \texttt{COMPILE\_ERROR}. Bei \texttt{INFRA\_FAIL} bleibt der größte Teil der 484 Übergänge in derselben Klasse (46,3\,\%), der Rest wechselt vor allem zu \texttt{ASSERTION\_FAIL} (30,8\,\%) oder direkt zu \texttt{PASS} (22,9\,\%).

Die schlechte Performance von UC-03 ist auf denselben Fehler zurückzuführen, der schon in Abschnitt~6.2 beschrieben wurde. 40 der 41 Abbrüche gehen auch hier auf die Weiterverwendung von \code|expect.poll| zurück. Bei UC-07 und UC-08 dominieren kartenbezogene Fehler. Abgefangene Pointer-Events treten in 122 fehlgeschlagenen Iterationen auf, davon 74 bei UC-07 und 24 bei UC-08.

In der Qualitätsbewertung erreicht Stufe~5 mit 3,96 in \emph{coverage}, 3,77 in \emph{selector}, 3,62 in \emph{assertion} und 3,06 in \emph{map\_interaction} die besten Mittelwerte aller Stufen. Die \emph{map\_interaction}-Werte der einzelnen Use Cases haben sich gegenüber den Stufen~3 und 4 allerdings kaum verbessert (Abbildung~\ref{fig:stufe5_score_heatmap}).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{stufe5_score_heatmap}
    \caption[Mittlerer Judge-Score je Use Case und Dimension der Bonus-Stufe 5]{Mittlerer Judge-Score je Use Case und Dimension der Bonus-Stufe~5 (Phase~2)}
    \label{fig:stufe5_score_heatmap}
\end{figure}

Von den bestandenen Tests sind 14 (3,8\,\%) als \texttt{vacuous\_pass} eingestuft. Sie treten ausschließlich bei UC-07 (8) und UC-08 (6) auf und entsprechen damit den Use Cases mit den kartenbezogenen Fehlern.

Der Ressourcenaufwand unterscheidet sich deutlich gegenüber den anderen Stufen. Über alle Iterationen entstanden 2.154 Generierungsaufrufe gegenüber 500 je Stufe. Zudem ist die Gesamtdauer der Generierung mit 15 h 1 min im Vergleich zu Stufe~4 mit 3 h 46 min deutlich länger und aufwendiger.

\subsection{GIS-spezifische Besonderheiten}

Während Abschnitt~6.3 die Karteninteraktion mit \emph{map\_interaction} semantisch bewertet, wird nun anhand deterministischer Codemuster analysiert, wie die generierten Tests mit dem Kartenzustand und der Karteninteraktion tatsächlich umgehen.

Ohne \ac{UI}-Kontext erfindet das Modell Selektoren. Sobald ein Kontext vorliegt, geschieht das kaum noch. In Stufe~1 verwenden 197 der 500 Dateien (39,4\,\%) mindestens einen \code|getByTestId|-Aufruf mit einem Wert, der in der Anwendung nicht existiert. Bereits in Stufe~2 sinkt dieser Anteil auf 2 Dateien (0,4\,\%). In den Stufen 3 und 4 bleibt er mit 1,0\,\% bzw. 1,8\,\% sehr niedrig.

Die verbleibenden Fälle der Stufen 2 bis 4 betreffen überwiegend die Druckfunktion in UC-09. Sie ist als fertige \ac{OPT}-Komponente eingebunden, weshalb ihre Elemente keine \code|data-testid| haben. Vergeben sind nur der Toolbar-Toggle für die Umstellung der Sichtbarkeit und das umgebende Panel. Für Titelfeld, Formatauswahl und Export-Button bleibt dem Modell damit nur der Rollen-Selektor oder die Erfindung.

Welchen Zugang die Tests zum Prüfen des Kartenzustands wählen, hängt davon ab, was der Kontext anbietet. In Stufe~1 bekommt das \ac{LLM} keine Informationen, weshalb 30,8\,\% der Dateien über \ac{CSS} auf das Canvas zugreifen, meistens mit \code|locator('canvas')|. Dieser Selektor ist allerdings mehrdeutig, weil OpenLayers mehrere Canvas-Elemente rendert~\parencite{cjhewett_ability_2020}, was in 70 Dateien unmittelbar zum Fehlschlag führt. Stufe~2 liefert mit \code|map-container| erstmals einen Namen für die Karte, sodass der Canvas-Zugriff auf 1,4\,\% einbricht, während \code|getByTestId('map-container')| von 13,0 auf 53,4\,\% steigt. Die Prüfung bleibt aber \ac{DOM}-basiert: Geprüft wird, dass das Kartenelement vorhanden ist, aber nicht, was die Karte zeigt. Erst Stufe~3 öffnet mit den Map-Model-Helfern den Kartenzustand selbst. 83,4\,\% der Dateien nutzen sie, in den Stufen~1 und 2 wird keine selbst erstellte Helferfunktion genutzt.\footnote{Die regelbasierte Auszählung weist für die Stufen 1 und 2 je eine Datei (0,2\,\%) aus. In beiden wird kein Helfer verwendet: in Stufe~1 steht der Funktionsname nur in einem Kommentar, in Stufe~2 ruft der Test ein selbst erfundenes Global \code|__getMapZoomLevel| auf. Die Suchmuster prüfen den bloßen Funktionsnamen und erfassen daher auch solche Stellen.} Tabelle~\ref{tab:kartenzustand} fasst die Anteile dieser Muster über die Stufen zusammen.

\begin{table}[H]
    \centering
    \caption[Prüfung des Kartenzustands je Stufe]{Anteil der Testdateien mit Canvas-Selektor, map-container-Selektor, Canvas-Assertion und Map-Model-Helfer-Nutzung}
    \label{tab:kartenzustand}
    \begin{tabular}{lrrrr}
        \toprule
        Muster & Stufe 1 & Stufe 2 & Stufe 3 & Stufe 4 \\
        \midrule
        \code|locator('canvas'/.ol-viewport)|      & 30{,}8\,\% & 1{,}4\,\%  & 0{,}4\,\%  & 0{,}4\,\% \\
        \code|getByTestId('map-container')|        & 13{,}0\,\% & 53{,}4\,\% & 50{,}1\,\% & 31{,}8\,\% \\
        Assertion auf map-container/Canvas         & 35{,}2\,\% & 32{,}2\,\% & 29{,}7\,\% & 7{,}0\,\% \\
        Nutzung eines Map-Model-Helfers             & 0{,}0\,\% & 0{,}0\,\% & 83{,}4\,\% & 74{,}0\,\% \\
        \bottomrule
    \end{tabular}
\end{table}

In UC-04 wird erwartet, dass der Layer auf der Karte gerendert wird. Ohne Map-Model weichen die Tests auf Elemente im \ac{DOM} aus. In Stufe~1 prüfen alle 50 UC-04-Dateien den Zustand der Checkbox und sonst nichts. In Stufe~2 kommt in 49 Dateien der Legendeneintrag als zusätzliche Prüfung hinzu. Ab Stufe~3 nutzen alle UC-04-Dateien \code|isLayerRendered|, und der Legendeneintrag verschwindet wieder weitgehend, die Checkbox-Prüfung bleibt aber erhalten.

Der Klick in die Karte bleibt in allen Stufen schwach. UC-06 und UC-07 verlangen beide eine Klick-Interaktion mit der Karte, einmal beliebig und einmal mit vorgegebener Koordinate. Playwright klickt jedoch Pixel relativ zur Elementecke, und für die Umrechnung stellt der Kontext keine Funktion bereit. In vielen Fällen rechnet das Modell die Koordinate in UC-07 nicht um, sondern übergibt die Kartenkoordinate einfach als Pixelposition. In Stufe~2 betrifft das 41 der 50 UC-07-Tests. Die Tests scheitern dann, da die Klickposition weit außerhalb des Elements liegt. In den bestandenen UC-07-Tests der Stufen~3 und 4 wird die Koordinate korrekt in Pixel umgerechnet.

UC-06 scheitert aus einem anderen Grund. In Stufe~3 rufen alle 50 UC-06-Dateien \code|getHighlightedCoordinate| auf und prüfen den Rückgabewert auf Existenz. Diese Prüfung schlägt in 32 der 50 Dateien fehl, weil der Wert \code|undefined| ist, was darauf schließen lässt, dass der Klick die Karte nie erreicht hat. Die Bedienpanels sind jeweils als \code|MapAnchor| auf dem \code|MapContainer| verankert und liegen damit über der Kartenfläche. Links oben ist der Layer Switcher, rechts oben das Info Panel usw. (s. Abbildung~\ref{fig:demo_webgis_anwendung} in Kapitel~5.1). 24 der 50 Dateien in Stufe~3 klicken auf die Position (100, 100) und treffen damit das Layer-Switcher-Panel statt der Kartenfläche.

Stufe~5 zeigt, dass der Loop diese Fehler teilweise selbst behebt. Die kartenbezogenen Use Cases starten in der initialen Iteration nahe bei null: UC-06 besteht 1 von 50 Läufen, UC-07 und UC-08 keinen einzigen. In Abbildung~\ref{fig:stufe5_loop_ergebnis} ist zu erkennen, dass diese Use Cases, wenn sie erfolgreich sind, dies erst in späteren Iterationen erreichen. UC-07 benötigt im Schnitt 7,8 und UC-08 8,5 Iterationen, während die rein \ac{DOM}-basierten UC-01, UC-02 und UC-05 mit 1,0 bis 1,3 Iterationen auskommen.

Besonders deutlich wird das bei UC-08, bei dem eine Linienmessung auf der Karte verlangt wird. In den Stufen 1 bis 4 bleibt die Bestehensquote mit 0\,\%, 0\,\%, 8\,\% und 12\,\% sehr niedrig. Die Ursache liegt schon in der Interaktion und nicht erst in der Prüfung. Die Tests öffnen zwar das richtige Panel, es entsteht aber in vielen Fällen keine Messung, da das Messpanel bei Aktivierung an einer festen Position (420, 10) über der Karte gerendert wird und somit zum Hindernis für die Zeichnung wird. Auch in Stufe~5 bleibt dieses Grundproblem bestehen. Mit 28\,\% hat UC-08 dort nach UC-03 die schlechteste PASS-Rate. Er verlangt mehrere Klicks und einen Doppelklick zum Abschluss. Bleibt ein Stützpunkt an einem überlagernden Panel hängen, entsteht kein Fehler an der Klickstelle, sondern der Test scheitert erst am erwarteten Textmuster für das Messergebnis. Das erklärt auch die 30 von 50 \texttt{ASSERTION\_FAIL}-Fälle. Beobachtbar ist, dass das Modell daraufhin die Prüfung umschreibt, die Klickfolge selbst aber unverändert lässt und die eigentliche Ursache damit nicht behebt. Von den 14 bestandenen Läufen sind zudem 6 als \texttt{vacuous\_pass} identifiziert worden.