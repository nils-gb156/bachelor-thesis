\newpage
\section{Implementierung der Evaluationsumgebung}

\subsection{Demo-WebGIS-Anwendung}

Die Demo-WebGIS-Anwendung stellt eine realistische, aber kontrollierte Testumgebung für die Evaluation bereit. Als Single-Page-Webanwendung umgesetzt, dient sie der weltweiten Erkundung von Wetterdaten und -vorhersagen. Die Anwendung ist unter \url{https://nils-gb156.github.io/ba-webgis-llm-e2e/} erreichbar und kann auch lokal gestartet werden. Der Quellcode ist auf GitHub unter \url{https://github.com/nils-gb156/ba-webgis-llm-e2e} verfügbar.

Abbildung~\ref{fig:demo_webgis_anwendung} zeigt die Oberfläche der Anwendung. Den Mittelpunkt bildet die Karte. Auf der linken Seite befinden sich die Layersteuerung und die Legende, die über die Buttons in der Werkzeugleiste unten auch optional ein- und ausgeblendet werden können. Die Layersteuerung ermöglicht das Umschalten zwischen Hintergrundkarten sowie das Ein- und Ausblenden von Layern. Die Legende darunter zeigt die Symbolerklärungen der jeweils aktiven Layer.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{demo_webgis_anwendung}
    \caption[Demo-WebGIS-Anwendung]{Oberfläche der Demo-WebGIS-Anwendung mit geklickter Koordinate}
    \label{fig:demo_webgis_anwendung}
\end{figure}

Die Anwendung bietet drei verschiedene Hintergrundkarten: Carto Light (Standard), Carto Dark und OpenStreetMap. Initial sichtbar sind zwei Punktlayer des Deutschen Wetterdienstes (\acs{DWD})\acused{DWD} für EUCOS-Bodenstationen und UV-Index-Stationen. Außerdem ist noch ein flächendeckender Layer der aktuellen Temperatur von OpenWeatherMap sichtbar. Über die Layersteuerung können weitere flächendeckende Layer für Niederschlag und Wolkenbedeckung ebenfalls von OpenWeatherMap sowie ein europaweiter UV-Index-Layer des \ac{DWD} eingeblendet werden.

Ein Klick auf die Karte öffnet ein Infopanel, das den Ort und die Wettervorhersage für die nächsten drei Tage im Drei-Stunden-Takt anzeigt. Die insgesamt 24 Einträge werden über die OpenWeatherMap API abgerufen und enthalten jeweils eine kurze Beschreibung des Wetters, Temperatur, Luftfeuchtigkeit sowie Windrichtung und -geschwindigkeit. Wird zusätzlich auf eine Bodenstation oder UV-Index-Station geklickt, werden die Stationsinformationen aus der \ac{WMS}-GetFeatureInfo-Anfrage unterhalb der Wettervorhersage angezeigt.

Oben in der Mitte der Karte befindet sich ein Feld für eine Freitextsuche, die über die Nominatim-Geocoding-API nach Orten sucht. Bei Auswahl eines Suchergebnisses wird die Karte auf den Ort zentriert und das Infopanel mit der Wettervorhersage angezeigt.

Über die Werkzeugleiste lässt sich außerdem zur initialen Ausdehnung zurücknavigieren sowie der Kartenausschnitt vergrößern oder verkleinern. Zwei weitere Buttons rechts daneben öffnen verschiebbare Floating-Panels für Mess- und Druckfunktion. Das Messwerkzeug ermöglicht das direkte Messen von Linien und Flächen in der Karte. Mit der Druckfunktion kann der aktuelle Kartenausschnitt mit allen aktiven Layern als \ac{PDF} oder \ac{PNG} exportiert werden. Im Footer werden die aktuellen Kartenkoordinaten in \ac{WGS84} sowie eine Maßstabsleiste und der Maßstab angezeigt.

Die Anwendung wurde mit Open Pioneer Trails entwickelt und basiert auf OpenLayers, React, Chakra UI und TypeScript. Sie nutzt die verfügbaren OPT-Pakete für Layersteuerung, Kartennavigation, Legende, Messung, Druck und Koordinatenanzeige. Die Kartenprojektion ist \ac{EPSG}:3857, die Koordinatenanzeige im Footer transformiert diese zu \ac{EPSG}:4326.

\subsection{Use-Case-Sammlung}

Für das Experiment wurden zehn Use Cases erstellt, die typische Nutzerinteraktionen mit der Demo-WebGIS-Anwendung abbilden. Sie folgen dem in Kapitel~4.2 beschriebenen Format und verteilen sich auf drei einfache, zwei mittlere und fünf schwere Use Cases. Tabelle~\ref{tab:use-cases} zeigt eine Übersicht.

\input{kapitel/kapitel_4/use_cases_tabelle}

Die einfachen Use Cases prüfen einzelne Bedienelemente über den \ac{DOM}, etwa das Ein- und Ausblenden der Layersteuerung, den Wechsel der Hintergrundkarte oder die Bedienung der Zoom-Buttons. Die mittleren Use Cases aktivieren zunächst ausgeblendete Layer und prüfen anschließend einen daraus resultierenden Zustand. Bei UC-4 ist es das tatsächliche Rendern der Kacheln auf der Karte und bei UC-5 die Aktualisierung der Legende.

Die schweren Use Cases hingegen erfordern Karteninteraktionen, asynchrone Prozesse oder das Zusammenspiel mehrerer Komponenten. UC-6 und UC-7 wurden trotz oberflächlicher Ähnlichkeit bewusst getrennt gehalten. UC-6 prüft die Wettervorhersage durch einen Klick auf eine beliebige Position auf der Karte, während UC-7 das gezielte Treffen einer vorgegebenen Koordinate erfordert. Hierbei muss das \ac{LLM} die Koordinate zuerst in Pixelkoordinaten umrechnen, bevor der Klick simuliert werden kann. UC-8 (Streckenmessung) verlangt eine mehrstufige Zeichnung auf dem Canvas inklusive Doppelklick zum Abschluss und UC-9 prüft den \ac{PNG}-Export der aktuellen Kartenansicht. Das Besondere an diesen beiden Use Cases ist, dass diese Funktionen direkt über \ac{OPT}-Komponenten eingebunden sind. Bei der Erstellung der WebGIS-Anwendung besteht daher kein Einfluss darauf, wie im Test mit diesen Komponenten interagiert werden kann. Der komplexeste ist UC-10, da er Layer-Konfiguration, Geocoder-Suche, Kartennavigation und Wettervorhersage in einem einzigen Ablauf kombiniert.

Da es sich um eine Demo-Anwendung handelt, ist die Sammlung bewusst begrenzt gehalten. Reale WebGIS-Anwendungen können in der Praxis deutlich umfangreichere Interaktionen erfordern.

\subsection{Manuell erstellte Referenztests}

Für jeden Use Case wurde manuell ein Playwright-Test erstellt. Die Erstellung erfolgte vor der LLM-Generierung, um eine Beeinflussung zu vermeiden. Wären die Referenztests nach den generierten Tests geschrieben, bestünde die Gefahr, sich unbewusst an deren Lösungen zu orientieren. Die Referenztests haben zwei Funktionen. Zum einen dienen sie als fachlich korrekte Referenzimplementierung, die zeigt, wie ein Use Case sauber umgesetzt aussieht. Zum anderen bilden sie den Vergleichsmaßstab für die LLM-generierten Tests in der Evaluation (Kapitel~6).

Die Tests sind mit Playwright in TypeScript umgesetzt. Pro Use Case gibt es eine eigene Spec-Datei nach dem Namensschema \texttt{uc-01-*.spec.ts} bis \texttt{uc-10-*.spec.ts}. Jeder Test folgt demselben Aufbau, der die Struktur des Use Cases widerspiegelt: Zuerst wird geprüft, ob die Vorbedingungen erfüllt sind, dann werden die Schritte ausgeführt und zuletzt die erwarteten Ergebnisse überprüft. Listing~\ref{lst:uc-04-uv-index-overlay} zeigt diesen Aufbau am Beispiel von Use Case~4.

Bevor ein Test mit der Webanwendung interagiert, muss sichergestellt sein, dass die Anwendung vollständig geladen ist. Statt einer festen Wartezeit (Timeout), die unzuverlässig ist, wird mit \code|page.waitForFunction(() => globalThis.__openPioneerMap != null)| gewartet, bis das Kartenmodell verfügbar ist. Sobald das Objekt existiert, ist die Karte einsatzbereit. Der Test wartet damit auf ein deterministisches Signal und nicht auf eine geschätzte Dauer. Dieses Muster ist in jedem Test enthalten.

\lstinputlisting[
    language=TypeScript,
    caption={UseCase-4: UV-Index Layer einblenden und prüfen, ob es auf der Karte angezeigt wird (Playwright-Test)},
    label={lst:uc-04-uv-index-overlay}
]{Quellcode/uc-04-uv-index-overlay.spec.ts}

Die Locator-Strategie basiert auf der in Kapitel~2 beschriebenen Hierarchie und nutzt bevorzugt \code|getByTestId|, dann \code|getByRole| und dann \code|getByText|. \ac{CSS}-Selektoren und XPath werden vermieden, da sie an die \ac{DOM}-Struktur gebunden und damit fragil sind. Die Selektoren müssen zudem bewusst präzise gewählt werden, da es sonst zu Fehlzugriffen kommen kann. Zum Beispiel wird in Use Case~4 der UV-Index Layer über \code|getByRole("checkbox", { name: "UV-Index", exact: true })| angesprochen. Die Option \code|exact| ist hier notwendig, weil sonst auch der Layer \enquote{UV-Index Stations} mitgetroffen werden würde, was im Rahmen des Use Cases falsch wäre. Eine Ausnahme ist Use Case~9. Dort werden zwei Elemente über \ac{CSS}-Klassen angesprochen, weil die betreffenden internen Elemente der Druckfunktion von Open Pioneer Trails keine eigene Test-ID besitzen.

Da der Karteninhalt auf einem Canvas gerendert wird (siehe Kapitel~2.4), gibt es dort keine \ac{DOM}-Elemente. Klicks auf der Karte erfolgen daher über \code|page.mouse.click(x, y)| mit Pixelkoordinaten, die aus der \code|boundingBox()| des Kartencontainers berechnet werden. Dabei gibt es einen wichtigen Unterschied zwischen zwei Arten von Kartenklicks. Bei Use Case~6 genügt ein Klick auf eine beliebige Position, etwa die Kartenmitte. Bei Use Case~7 hingegen muss ein konkreter Punkt getroffen werden. Dafür wird die bekannte Koordinate (\ac{EPSG}:3857) über die OpenLayers-Funktion \code|getPixelFromCoordinate()| in eine Canvas-Pixelposition umgerechnet, bevor geklickt wird. Das gezielte Treffen eines Features ist damit deutlich aufwendiger als ein beliebiger Klick in die Karte.

Der Kartenzustand, dazu gehören aktive Layer, Zoomstufe, Zentrum und Highlight, ist nicht über das \ac{DOM} auslesbar. In der Datei \texttt{map-model-helpers.ts} sind Funktionen, die auf das MapModel zugreifen. Sie sind bewusst ausgelagert, da sie unabhängig von der Webanwendung sind und auch in anderen Open Pioneer Trails-Anwendungen wiederverwendet werden können. Passend für die Use Cases stellt die Datei aktuell die Funktionen \code|getActiveBaseLayerTitle()|, \code|isLayerRendered()|, \code|getMapZoomLevel()|, \code|getMapCenter()| und \code|getHighlightedCoordinate()| bereit. Die Funktionen müssen nur eingebunden werden und können dann direkt im Test verwendet werden.

Zum Beispiel geht es in Use Case~4 um das Aktivieren des UV-Index Layers. Dies kann nun relativ einfach auf zwei Ebenen geprüft werden. Erstens auf DOM-Ebene, dass der Toggle in der Layersteuerung den Zustand \enquote{aktiviert} (\code|toBeChecked|) hat. Zweitens auf Modell-Ebene, dass der Layer auch tatsächlich gerendert wird (\code|isLayerRendered|). Nur weil ein Layer in der Layersteuerung aktiviert wurde, bedeutet das noch nicht, dass die Kacheln auch wirklich auf der Karte erscheinen. Die Prüfung auf Modell-Ebene nutzt \code|expect.poll()|, da die Kacheln asynchron über das Netz geladen werden. Ein reines \code|expect| ist hier nicht ausreichend, da es nur einen festen Wert prüft und nicht wiederholt nachfragt. Mit \code|expect.poll()| wird die Funktion wiederholt aufgerufen, bis der Layer als gerendert gilt oder ein Timeout erreicht wird. Somit wird sichergestellt, dass die Kacheln geladen sind, bevor der Test fortschreitet. Konkret sieht das dann so aus: \code|await expect.poll(() => isLayerRendered(page, UVI_LAYER_TITLE)).toBe(true)|.

In Use Case~7 wird zusätzlich überprüft, dass tatsächlich ein \ac{WMS}-GetFeatureInfo-Request an den \ac{DWD}-Dienst gesendet wird. Dazu werden mit \code|page.on("request", ...)| die ausgehenden Requests beobachtet.
